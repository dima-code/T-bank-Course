
/*
Задание:
Создать экран с анимацией появления элементов и эффектами, используя полученные знания по анимации.
Описание:
1. 3 элемента UI:
  Логотип (`UIImageView`)
  Заголовок (`UILabel`)
  Кнопка действия (`UIButton`)
2. Анимация появления. Элементы должны появляться с анимацией:
  Логотип: выезжает сверху
  Заголовок: появляется с fade (alpha)
  Кнопка: scale + небольшое вращение
3. Использовать:
  `UIView.animate(...)`
  `transform` (для кнопки)
  `alpha`
  `delay` для создания последовательности
4. Дополнительные эффекты:
    Анимация тени у кнопки через `CABasicAnimation`
    Анимация `cornerRadius` при наведении / нажатии
 
Примерный порядок анимации:
    Логотип выезжает сверху (`transform + alpha`)
    Через 0.2 сек — fade-заголовок (`alpha`)
    Через 0.4 сек — кнопка с bounce-эффектом (`transform`, `rotation`, `scale`)
 
Формат сдачи:
    Xcode проект
    Комментарии в коде, указывающие на каждую анимацию

*/
